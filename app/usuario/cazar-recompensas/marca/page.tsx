"use client"; import Link from "next/link"; import { ArrowLeft,ChevronRight,ReceiptText,Store } from "lucide-react"; import { useEffect,useState } from "react"; import styles from "../CazarRecompensas.module.css"; import { readActiveDynamicCampaigns,type BrandCampaign,DYNAMIC_CAMPAIGNS_EVENT } from "@/lib/campaignDynamics";
export default function Page(){const [items,setItems]=useState<BrandCampaign[]>([]);useEffect(()=>{const u=async()=>setItems(await readActiveDynamicCampaigns("brand") as BrandCampaign[]);void u();addEventListener(DYNAMIC_CAMPAIGNS_EVENT,u);return()=>removeEventListener(DYNAMIC_CAMPAIGNS_EVENT,u)},[]);return <main className={styles.mobileStage}><div className={styles.topBar}><Link href="/usuario/cazar-recompensas" className={styles.backButton}><ArrowLeft/></Link><span>Visita a marca</span></div><section className={styles.selectorIntro}><div className={styles.eyebrow}><ReceiptText/>Campañas activas</div><h1>Sube tu ticket</h1><p>La IA leerá la compra y validaremos que estés cerca de la sucursal.</p></section><section className={styles.campaignCarousel}>{items.map(c=><Link key={c.id} href={`/usuario/cazar-recompensas/marca/ticket?campaign=${c.id}`} className={styles.campaignCard}><div className={styles.campaignIcon}><Store/></div><div className={styles.campaignBody}><span>{c.brandName}</span><h2>{c.name}</h2><p>{c.description}</p></div><ChevronRight/></Link>)}</section>{!items.length&&<section className={styles.emptyCard}><Store/><h2>No hay campañas activas</h2><p>El administrador debe publicar una campaña de marca.</p></section>}</main>}
