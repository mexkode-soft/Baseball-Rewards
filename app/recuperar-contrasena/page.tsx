"use client";import Link from "next/link";import {FormEvent,useState} from "react";import {supabase} from "@/lib/supabase";
export default function Page(){const[email,setEmail]=useState("");const[msg,setMsg]=useState("");async function submit(e:FormEvent){e.preventDefault();const{error}=await supabase.auth.resetPasswordForEmail(email.trim(),{redirectTo:`${window.location.origin}/actualizar-contrasena`});setMsg(error?.message??"Revisa tu correo para restablecer la contraseña.")}return <main style={{minHeight:"100vh",display:"grid",placeItems:"center",background:"#07080a",color:"white",padding:24}}><form onSubmit={submit} style={{width:"min(440px,100%)",display:"grid",gap:16,background:"#101216",padding:28,borderRadius:20,border:"1px solid #2b2d33"}}><h1>Recuperar contraseña</h1><p>Te enviaremos un enlace seguro.</p><input type="email" value={email} onChange={e=>setEmail(e.target.value)} required style={{padding:14,borderRadius:12}}/><button style={{padding:14,borderRadius:12,background:"#f4c542",fontWeight:800}}>Enviar enlace</button>{msg&&<p>{msg}</p>}<Link href="/login">Volver al inicio de sesión</Link></form></main>}
