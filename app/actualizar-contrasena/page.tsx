"use client";import {FormEvent,useState} from "react";import {useRouter} from "next/navigation";import {supabase,getCurrentRole} from "@/lib/supabase";
export default function Page(){const[p,setP]=useState("");const[msg,setMsg]=useState("");const router=useRouter();async function submit(e:FormEvent){e.preventDefault();const{error}=await supabase.auth.updateUser({password:p});if(error){setMsg(error.message);return}setMsg("Contraseña actualizada.");const role=await getCurrentRole();router.replace(role==="admin"?"/admin":role==="sponsor"?"/patrocinador":"/usuario")}return <main style={{minHeight:"100vh",display:"grid",placeItems:"center",background:"#07080a",color:"white",padding:24}}><form onSubmit={submit} style={{width:"min(440px,100%)",display:"grid",gap:16,background:"#101216",padding:28,borderRadius:20,border:"1px solid #2b2d33"}}><h1>Nueva contraseña</h1><input type="password" minLength={8} value={p} onChange={e=>setP(e.target.value)} required placeholder="Mínimo 8 caracteres" style={{padding:14,borderRadius:12}}/><button style={{padding:14,borderRadius:12,background:"#f4c542",fontWeight:800}}>Guardar contraseña</button>{msg&&<p>{msg}</p>}</form></main>}
